name: Thesis Fair Platform
endpoint: amqp://rabbitmq
datasets:
  db:
    type: mongodb
    url: mongodb://dataset-mongodb
    db: thesisFairPlatform
    collection: users
userStories:
- given: a http get request with path "/user/login" on port 3000 with parameter "username" of type "string" and a parameter "password" of type "string"
  then:
  - pre:
      select:
      - username
    do: query dataset "db" find one match state
    post:
      # unset:
      # - key
      upsert:
      - password as dbpwd
      # set: completeResultInThisKey
  - given: dbpwd equals `undefined`
    then:
    - set state "User not found"
    - given: state equals "Hello there"
      then:
      - stop
    - respond to the http request on port 3000
    - stop
  - log state
  - respond to the http request on port 3000

    # do: query mongodb database at "mongodb" using "username" and "password", add "result.password" to the state as "databasePassword"


# - given: a http get request with path "/user/register" on port 3000 with parameter "username" of type "string" and a parameter "password" of type "string" and a parameter "email" of type "string"
#   then:
#   - store in a mongodb database at "mongodb"
